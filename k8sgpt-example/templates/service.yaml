apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8sgpt-example.fullname" . }}
  labels:
    {{- include "k8sgpt-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- toYaml .Values.selectorLabels | nindent 4 }}
    {{- include "k8sgpt-example.selectorLabels" . | nindent 4 }}
